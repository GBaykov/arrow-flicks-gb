import { AppLayout } from '@pages/layout';
import { FC } from 'react';

export const RatedMoviesPage: FC = () => {
    return (
        <AppLayout>
            <div>RATED</div>
        </AppLayout>
    );
};
